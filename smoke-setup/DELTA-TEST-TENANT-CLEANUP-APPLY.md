# DELTA — Test-Tenant Policy Cleanup Apply Report

**Apply timestamp:** Wed 2026-06-10 22:02 EDT  
**Migration file:** `~/casabe-v3/smoke-setup/test-tenant-policy-fix.sql`  
**DB target:** Supabase (aws-1-us-east-1) — `public.orders`

---

## Migration Applied: ✅ SUCCESS

```
BEGIN
DROP POLICY
DROP POLICY
DROP POLICY
COMMIT
```

Transaction completed atomically. All three DROP POLICY IF EXISTS statements executed within a single BEGIN/COMMIT block with no errors.

---

## Verify 1 — Named policies gone: ✅ PASS (0 rows)

Query:
```sql
SELECT policyname, roles, cmd
FROM pg_policies
WHERE tablename = 'orders'
  AND policyname IN (
    'test_tenant_anon_read',
    'test_tenant_anon_insert',
    'test_tenant_anon_update'
  );
```

Result:
```
 policyname | roles | cmd 
------------+-------+-----
(0 rows)
```

**Expected:** 0 rows — ✅ PASS

---

## Verify 2 — No anon policies remain on orders: ✅ PASS (0 rows)

Query:
```sql
SELECT policyname, roles, cmd
FROM pg_policies
WHERE tablename = 'orders'
  AND 'anon' = ANY(roles);
```

Result:
```
 policyname | roles | cmd 
------------+-------+-----
(0 rows)
```

**Expected:** 0 rows — ✅ PASS

---

## Overall Verdict: ✅ APPLIED AND VERIFIED

All three anonymous order policies for `test-tenant` have been removed from `public.orders`. No anon policies remain on the table. Rollback was NOT required.

---

*Report generated by Delta (QA/Debugger) — Casabe Konnect*
